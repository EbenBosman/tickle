{
  "name": "tickle",
  "version": "0.1.0",
  "private": true,
  "description": "Local AI browser agent — root harness for server + web.",
  "scripts": {
    "install:all": "npm install && npm install --prefix server && npm install --prefix web",
    "dev": "concurrently --names server,web --prefix-colors blue.bold,magenta.bold --kill-others-on-fail \"npm --prefix server run dev\" \"npm --prefix web run dev\"",
    "dev:server": "npm --prefix server run dev",
    "dev:web": "npm --prefix web run dev",
    "test": "npm --prefix server test && npm --prefix web test",
    "test:server": "npm --prefix server test",
    "test:web": "npm --prefix web test",
    "typecheck:server": "npm --prefix server run typecheck",
    "typecheck:web": "npm --prefix web run typecheck",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "npm --prefix server run lint && npm --prefix web run lint",
    "lint:server": "npm --prefix server run lint",
    "lint:web": "npm --prefix web run lint",
    "test:coverage": "npm --prefix server run test:coverage && npm --prefix web run test:coverage",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{ts,tsx,js,mjs,cjs,json,md,yml,yaml}": [
      "prettier --write"
    ]
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "husky": "9.1.7",
    "lint-staged": "17.0.3",
    "prettier": "3.8.3"
  }
}
